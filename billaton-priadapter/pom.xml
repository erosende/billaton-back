<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.erosende</groupId>
        <artifactId>billaton</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>billaton-priadapter</artifactId>
    <name>${project.groupId}.${project.artifactId}</name>
    <packaging>${packaging.type}</packaging>
    <description>Módulo para la implementación de los adaptadores primarios</description>

    <dependencies>

        <dependency>
            <groupId>dev.erosende</groupId>
            <artifactId>billaton-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.3.0</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

    </dependencies>

</project>
